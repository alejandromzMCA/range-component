import NormalRange from "@/components/NormalRange"

const Exercise1 = () => {
    return <div style={{ display: "flex", marginTop: "3rem", justifyContent: "center", alignItems: "center" }}><NormalRange minDefault={0} maxDefault={100} /></div>
}

export default Exercise1